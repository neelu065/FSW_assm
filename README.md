# FSW_assm

### Instructions to run the code
- This code is build within package manager called 'uv'. Following is the command to install the same.

- curl -LsSf https://astral.sh/uv/install.sh | sh
- uv sync
- Activate the virtual venv using source .venv/bin/activate
- Run the code as: python app.py

