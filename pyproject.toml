[project]
name = "fsw-assm"
version = "0.1.0"
description = "module to submit the assignment"
readme = "README.md"
authors = [
    { name = "neelu065", email = "neelu065@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
fsw-assm = "fsw_assm:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
